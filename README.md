# python_smell_detection_ml
A tool (machine learning pipeline) that beats a generic baseline logistic regression at a classification problem. The problem is python code smell detection, specifically, long method code smell.
